# Hosts Configuration

define host {
    use                     linux-server
    host_name               prdx-dns101
    alias                   DNS Server
    address                 192.168.100.10
    hostgroups              linux-servers
}

define host {
    use                     linux-server
    host_name               prdx-db101
    alias                   Database Server
    address                 192.168.100.20
    hostgroups              linux-servers,database-servers
}

define host {
    use                     linux-server
    host_name               prdx-webserver101
    alias                   Web Server 1
    address                 192.168.100.31
    hostgroups              linux-servers,web-servers
}

define host {
    use                     linux-server
    host_name               prdx-webserver102
    alias                   Web Server 2
    address                 192.168.100.32
    hostgroups              linux-servers,web-servers
}

define host {
    use                     linux-server
    host_name               prdx-webserver103
    alias                   Web Server 3
    address                 192.168.100.33
    hostgroups              linux-servers,web-servers
}

define host {
    use                     linux-server
    host_name               prdx-haproxy101
    alias                   Load Balancer
    address                 192.168.100.40
    hostgroups              linux-servers,loadbalancer-servers
}

define host {
    use                     linux-server
    host_name               prdx-nagios101
    alias                   Nagios Server
    address                 192.168.100.50
    hostgroups              linux-servers
}

define host {
    use                     linux-server
    host_name               prdx-ansible101
    alias                   Ansible Server
    address                 192.168.100.60
    hostgroups              linux-servers
}

define host {
    use                     linux-server
    host_name               prdx-dprimary101
    alias                   Docker Primary
    address                 192.168.100.70
    hostgroups              linux-servers
}

define host {
    use                     linux-server
    host_name               prdx-kube101
    alias                   Kubernetes Master
    address                 192.168.100.80
    hostgroups              linux-servers
}
